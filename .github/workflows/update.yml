name: Update PhD Progress

on:
  schedule:
    - cron: '0 0 * * *' # Run every day at midnight

jobs:
  update-progress:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Run Update Script
        run: |
          git config --global user.email "pvtien96@gmail.com"
          git config --global user.name "Tien PHAM"
          python update.py
          git add README.md
          git commit -m "Auto-update progress"
          git push
